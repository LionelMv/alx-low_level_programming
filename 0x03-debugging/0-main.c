#include "main.h"
/**
 * main - checks to print if an integer is positive or negative
 * @i: variable to be tested.
 * Return: 0
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);
	return (0);
}
